<animated-vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_profile">
    <!-- Target the group instead of the path -->
    <target
        android:name="profile_group"
        android:animation="@anim/rotate" />
</animated-vector>